spring.application.name=profesor-service
server.port=${PORT:8082}

# Comunicación con alumno-service - URL pública de Render
alumno.url=${ALUMNO_SERVICE_URL:https://dwp-alumno-service.onrender.com}

# Base de datos - usando variables de entorno para mayor seguridad
spring.datasource.url=${DATABASE_URL:jdbc:mysql://alumno-service-microservices-bjlp.c.aivencloud.com:16513/defaultdb?sslMode=REQUIRED&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:avnadmin}
spring.datasource.password=${DATABASE_PASSWORD:AVNS_uA_XUYoeCCWhWVE7IzF}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.hibernate.ddl=true
spring.jpa.show-sql=${SHOW_SQL:false}

# Logging
logging.level.org.hibernate.SQL=${LOG_LEVEL:warn}
logging.level.root=${ROOT_LOG_LEVEL:info}